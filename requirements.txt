booru
PyGObject
desktop-notifier
filetype
get-video-properties
pillow
pynput
pypresence
evdev
requests
screeninfo
voluptuous
websockets

# Optional: AI region detection for the denial/censor feature (pulls onnxruntime,
# downloads a model on first use). Without it, censoring falls back to whole-image.
# nudenet
